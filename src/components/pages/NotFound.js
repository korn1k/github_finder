import React, { Fragment } from 'react';

const NotFound = () => (
  <Fragment>
    <h1>Ooops! It seems there is nothing to show you</h1>
    <p>there could be something wrong with your request</p>
  </Fragment>
);

export default NotFound;
